import { App.Router.Module.Ts } from './app.router.module.ts';

describe('App.Router.Module.Ts', () => {
  it('should create an instance', () => {
    expect(new App.Router.Module.Ts()).toBeTruthy();
  });
});
